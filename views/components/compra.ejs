
<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= produto.nom_produtos %></title>
    <link rel="stylesheet" href="/css/compra.css">
    <link rel="shortcut icon" href="/imagens/logos/logo.png" type="image/x-icon">
</head>
<body>
    <header>
        <div class="back">
            <a href="/">
                <img src="/imagens/logos/logo-2.png" alt="mim" srcset="">
            </a>
        </div>
    </header>
    <% if (typeof mensagem !== 'undefined') { %>
        <div class="success-message">
            <p><%= mensagem %></p>
        </div>
    <% } %>
   
    <main>
        <div class="product-details">
            <img src="<%= produto.img_produto %>" alt="<%= produto.nom_produto %>">
        </div>


        
        <form action="/finalizar-compra" method="POST">
            <input type="hidden" name="id_produto" value="<%= produto.id_produtos %>">
            <h2><%= produto.nom_produtos %></h2>
            <p>Preço: R$ <%= parseFloat(produto.val_produtos).toFixed(2) %></p>
            <p>Quantidade disponível:  <%= produto.qtd_produtos %> </p>
            <input type="number" placeholder="Digite"
            id="quantidade" name="quantidade" min="1" max="<%= produto.qtd_produtos %>" required>




            <label for="tipo-pagamento">Tipo de pagamento:</label>
            <select id="tipo-pagamento" name="tipo_pagamento" required>
                <option value="cartao_credito">Cartão de Crédito</option>
                <option value="boleto">Boleto</option>
                <option value="pix">Pix</option>
            </select>

            <button type="submit">Finalizar Compra</button>
        </form>
    </main>
</body>
</html>
